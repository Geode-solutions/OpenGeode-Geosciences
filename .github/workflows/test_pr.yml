name: Test PR

on:
  pull_request:

jobs:
  test:
    uses: Geode-solutions/actions/.github/workflows/cpp-test-pr.yml@master
    with:
      name: OPENGEODE_GEOSCIENCES
      repos: OpenGeode
    secrets: inherit
